# /etc/wsl.conf
# Per-instance WSL2 configuration file, read once when the instance boots.

[boot]

# Enabled so xrdp — installed as a systemd service — can be managed with
# systemctl (start/stop/restart). WSL2 skips systemd by default, which
# would leave systemctl unavailable and xrdp never starting.

systemd=true
