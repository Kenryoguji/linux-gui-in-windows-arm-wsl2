# /etc/xrdp/xrdp.ini (excerpt — only the modified line is kept)
# Full file is xrdp's default config, several hundred lines, otherwise untouched.

[Globals]
# Changed from the default 3389: already in use by another service on the
# Windows host, so xrdp failed to bind on startup.
port=3390